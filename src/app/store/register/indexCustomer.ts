import { RegisterEffects } from './register.effects';
import { registerFeatureKey, registerReducer } from './register.reducer';
export const registerRoot = {
  RegisterEffects,
  registerFeatureKey,
  registerReducer,
};
